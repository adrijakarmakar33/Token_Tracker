import type { Chain, Hex } from "viem";
import { type CreateSessionDataParams, type Session, type SessionGrantedPayload, type SessionParams } from "..";
import { type BuildUserOpOptions, type Transaction, type ZeroXgaslessSmartAccount } from "../../account";
import type { ISessionStorage } from "../interfaces/ISessionStorage";
export type CreateBatchSessionConfig = {
    /** The storage client to be used for storing the session data */
    sessionStorageClient: ISessionStorage;
    /** An array of session configurations */
    leaves: CreateSessionDataParams[];
};
export declare const createBatchSession: (smartAccount: ZeroXgaslessSmartAccount, sessionKeyAddress: Hex, sessionStorageClient: ISessionStorage, leaves: CreateSessionDataParams[], buildUseropDto?: BuildUserOpOptions) => Promise<SessionGrantedPayload>;
declare const types: readonly ["ERC20", "ABI"];
export type BatchSessionParamsPayload = {
    params: {
        batchSessionParams: SessionParams[];
    };
};
export type SessionValidationType = (typeof types)[number];
/**
 * getBatchSessionTxParams
 *
 * Retrieves the transaction parameters for a batched session.
 *
 * @param sessionTypes - An array of session types.
 * @param transactions - An array of {@link Transaction}s.
 * @param session - {@link Session}.
 * @param chain - The chain.
 * @returns Promise<{@link BatchSessionParamsPayload}> - session parameters.
 *
 */
export declare const getBatchSessionTxParams: (sessionValidationTypes: SessionValidationType[], transactions: Transaction[], { sessionID, sessionStorageClient }: Session, chain: Chain) => Promise<BatchSessionParamsPayload>;
export {};
//# sourceMappingURL=batch.d.ts.map