import { type Chain, type Hex } from "viem";
import { type SmartAccountSigner, WalletClientSigner } from "../../account";
import type { ISessionStorage, SessionLeafNode, SessionSearchParam, SessionStatus } from "../interfaces/ISessionStorage";
import type { SignerData } from "../utils/Types";
export declare class SessionFileStorage implements ISessionStorage {
    smartAccountAddress: Hex;
    constructor(smartAccountAddress: Hex);
    private readDataFromFile;
    private getStorageFilePath;
    private writeDataToFile;
    private validateSearchParam;
    private getSessionStore;
    private getSignerStore;
    private toLowercaseAddress;
    getSessionData(param: SessionSearchParam): Promise<SessionLeafNode>;
    addSessionData(leaf: SessionLeafNode): Promise<void>;
    updateSessionStatus(param: SessionSearchParam, status: SessionStatus): Promise<void>;
    clearPendingSessions(): Promise<void>;
    addSigner(signerData: SignerData | undefined, chain: Chain): Promise<SmartAccountSigner>;
    getSignerByKey(sessionPublicKey: string, chain: Chain): Promise<WalletClientSigner>;
    getSignerBySession(param: SessionSearchParam, chain: Chain): Promise<WalletClientSigner>;
    getAllSessionData(param?: SessionSearchParam): Promise<SessionLeafNode[]>;
    getMerkleRoot(): Promise<string>;
    setMerkleRoot(merkleRoot: string): Promise<void>;
}
//# sourceMappingURL=SessionFileStorage.d.ts.map